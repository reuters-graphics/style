import makeColorScheme from './../../makeColorScheme';

import brandScheme from './brand';
import dawnScheme from './dawn';
import khakiScheme from './khaki';
import nordScheme from './nord';

export const brand = makeColorScheme(brandScheme);
export const dawn = makeColorScheme(dawnScheme);
export const khaki = makeColorScheme(khakiScheme);
export const nord = makeColorScheme(nordScheme);
