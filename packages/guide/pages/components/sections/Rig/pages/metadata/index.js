import Content from 'Content/graphics-rig/metadata/page.md';
import withRenderers from '../../../../common/markdown/withRenderers';

const Metadata = () => withRenderers(Content);

export default Metadata;
