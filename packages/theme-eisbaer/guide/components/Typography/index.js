import Copy from './copy.md';
import Copyable from '../../Copyable';
import React from 'react';

const Typography = () => (
  <section className='body-text'>
    <Copy />
  </section>
);

export default Copyable(Typography);
